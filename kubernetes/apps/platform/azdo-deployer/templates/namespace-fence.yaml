{{/*
  Included verbatim via .Files.Get, which does no template processing, so the
  Kyverno JMESPath variables in files/namespace-fence.yaml survive untouched.
  Helm parses actions even inside YAML comments, so the include must be the only
  action in this file -- hence a second template beside policies.yaml rather
  than two includes in one.
*/}}
{{ .Files.Get "files/namespace-fence.yaml" }}
